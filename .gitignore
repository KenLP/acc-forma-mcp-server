node_modules/
dist/
package-lock.json
.env
*.pem
*.key
coverage/
.DS_Store
*.log
audit-*.jsonl
*.db

# SEA (standalone executable) build outputs — distributed via GitHub Release,
# never committed (see scripts/sea-publish.mjs + npm run sea:publish)
dist-cjs/
forma-mcp.exe
forma-mcp-linux

# Claude Code per-user permissions (do not commit)
.claude/settings.local.json

# Local audit data (default FORMA_AUDIT_DIR)
.acc-forma-mcp/

# Internal planning & changelog (kept local; not for public repo)
CHANGELOG.md
docs/PLANNING.md
docs/REMEDIATION-PLAN.md

# Local review/analysis artifacts and live-test diagnostic scripts
REVIEW_FINDINGS*.md
scripts/diag-*.ts
scripts/test-*.ts
scripts/probe-*.ts

# Research artifacts (downloaded SDK/HTML files from agent research passes)
*.gen.cs
*.html

# Personal Claude Code skill configs and local session notes (not for public)
skills/
CLAUDE.md
.claude/LOCAL.md

# Dockerfile scaffold (not used yet; revisit when adding container distribution)
Dockerfile

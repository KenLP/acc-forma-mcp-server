node_modules/
dist/
package-lock.json
.env
*.pem
*.key
coverage/
.DS_Store
*.log
audit-*.jsonl
*.db

# SEA (standalone executable) build outputs — distributed via GitHub Release,
# never committed (see scripts/sea-publish.mjs + npm run sea:publish)
dist-cjs/
forma-mcp.exe
forma-mcp-linux

# Claude Code per-user permissions (do not commit)
.claude/settings.local.json

# Local audit data (default FORMA_AUDIT_DIR)
.acc-forma-mcp/

# Internal planning & changelog (kept local; not for public repo)
# Anything about plans, roadmap, or spinning this into other products stays local.
CHANGELOG.md
docs/PLANNING.md
docs/REMEDIATION-PLAN.md
docs/PAPER_OUTLINE.md

# Working notes: handoffs, backlogs, submission prep, specs written for a later
# session. These are our working conversation, not product documentation.
docs/HANDOFF.md
docs/BACKLOG-*.md
docs/MCP-PUBLISHER-SUBMISSION.md
docs/specs/

# Local review/analysis artifacts and live-test diagnostic scripts
REVIEW_FINDINGS*.md
scripts/diag-*.ts
scripts/test-*.ts
scripts/probe-*.ts

# Research artifacts (downloaded SDK/HTML files from agent research passes)
*.gen.cs
*.html
# ...but the landing page is a real deliverable, not a research artifact
!site/*.html

# Personal Claude Code skill configs and local session notes (not for public)
skills/
CLAUDE.md
.claude/LOCAL.md

# change-alert prototype runtime state
prototypes/change-alert/.state.json

# MCP Bundle (.mcpb) — packed output, rebuilt via bundle/README.md; bundle/server/node_modules
# and bundle/server/package-lock.json are already covered by the node_modules/ and
# package-lock.json rules above
*.mcpb
